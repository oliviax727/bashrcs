Fill in qssh module
Fix linting errors
Copy over Sirius quick scripts

(add autoconnect) ssh-copy-id -f -i ~/.ssh/[KEY_NAME] ohrw@setonix.pawsey.org.au

-f flag is overwrite and -i flag is to add specific key

Other qssh ideas:
- Add ssh key generation and switching (qssh create, qssh switch, -k option)
- Add this module to the setonix and other computer

Create branches:
- dev
- release/beta-1.0-main
- release/beta-1.0-clean
